# 상세페이지 템플릿 시스템 — 작업 요약

> 한국형 세로 상세페이지(폭 860px)를 **Figma 편집 레이어 + HTML 프리뷰**로 동시에 생성하는 블록 스펙 시스템.
> 단일 진실원천(스펙) → ① Figma 플러그인 ② 검증용 PNG.

## 1. 무엇을 만들었나

### (A) 레퍼런스 16종 재현 (`src/templates/specs/`)
초장문 4장(뷰티A/B/C·프리미엄A) 제외, **식품1부터 16종**을 단일 연속 페이지로 충실히 재현.
모든 섹션·색·폰트·뱃지·아이콘그리드·표·CTA를 축약 없이 복제(사진만 placeholder).

| 카테고리 | 템플릿 |
|---|---|
| 식품 | 식품1~5 (그래놀라·쭈꾸미·만두·떡볶이·쿠키) |
| 전자기기 | 밥솥·선풍기·에어프라이기·족욕기·가습기 |
| 패션 | 패션1(캡모자)·패션2(헤어커치프) |
| 반려동물&유아 | 유아3·유아4·유아5·유아A |

### (B) 실제 이미지 기능 (`kit.ts` + 런타임/프리뷰/빌드)
사진을 회색 placeholder가 아닌 **진짜 이미지**로:
- **풀블리드 배경** `p.bgimage()` + **그라데이션 스크림** `p.grad()` (글자 가독성)
- **슬롯 재배치/크롭** `p.photo({src, fit, focusX/Y})`
- **원형/라운드 마스킹** `{shape:'circle'}` / `radius`
- **단색 오버레이** `{overlay, overlayOpacity}`
- `src` 경로만 적으면 빌드 시 base64 자동 임베드 → Figma·HTML 동일 적용.
- 사용법: `inputs/images/README.md`

### (C) 섹션 패턴 카탈로그 (`src/templates/sections.ts`)
16종에서 반복되는 레이아웃을 **재사용 헬퍼 21종**으로 추출.
새 제품은 한 템플릿을 복제하지 않고 **여러 섹션을 골라 조립**한다.

- 히어로 4종: `heroEmblem` `heroBand` `heroBigType` `heroFullbleed`(실사)
- 구성: `sectionLabel` `stepsRow` `circleCaptionGrid` `iconGrid` `pointCards`
- 설득: `promiseBand` `reviewPills` `reviewCards` `ratingCard` `compareCard` `checklist`
- 특징: `featureLR` `featureStack` `recommendCards`
- 마감: `specRows` `infoPillRows` `ctaBand`
- 데모/메뉴: `npx tsx src/templates/catalog-demo.ts` → `output/templates/_카탈로그.png`

## 2. 빌드 & 확인

```bash
npx tsx src/templates/build.ts --png   # 16종 → figma-templates/code.js + output/templates/*.png
npx tsx src/templates/catalog-demo.ts   # 섹션 카탈로그 데모 PNG
```

**피그마 확인(데스크톱 앱):** Plugins → Development → Import plugin from manifest →
`figma-templates/manifest.json` 선택 → 실행하면 16개 프레임이 편집 레이어로 생성.
폰트: Pretendard / Nanum Pen Script / Noto Serif KR 설치 필요.

## 3. 핵심 파일

```
src/templates/
  kit.ts            # P 클래스 (y커서 빌더) + 이미지 헬퍼
  sections.ts       # 섹션 패턴 카탈로그 21종  ← 새 제품 조립의 핵심
  preview.ts        # 스펙 → HTML
  figma-runtime.ts  # 스펙 → Figma 플러그인 런타임
  build.ts          # 빌드(이미지 base64 임베드 포함)
  catalog-demo.ts   # 섹션 카탈로그 데모
  specs/            # 16종 템플릿 + types.ts
inputs/images/      # 실제 제품 사진 넣는 곳
figma-templates/    # 생성물: code.js + manifest.json (Figma import 대상)
```

## 4. 새 제품 작업 흐름

1. `inputs/images/<제품>/`에 실제 사진을 넣는다.
2. 카테고리·특징·(가능하면 좋아하는 레퍼런스)를 바탕으로 `sections.ts`에서 섹션을 골라 새 스펙을 조립.
3. 색·폰트·밀도를 브랜드에 맞춤. 사진은 `src`로 연결.
4. 빌드 → 렌더 → 원본/레퍼런스와 비교 → 보정(자기검증 루프).
5. Figma에서 최종 확인·수정.

## 5. 남은 한계(정직)

- 색 추출 자동화·실제 폰트 실측·자동 시각비교 루프는 아직 도구화 전 (다음 단계 후보).
- 사진 *내용* 리터칭/재생성은 불가 — 실사 입력으로 대체.
- 완전 자율 최상위 아트디렉션은 변주안 2~3개 + 사람 선택으로 우회 권장.
